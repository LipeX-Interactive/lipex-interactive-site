import { createClient } from "https://esm.sh/@supabase/supabase-js@2.57.4";
const cfg=window.LIPEX_CONFIG||window.LIPEX_SITE_CONFIG||{};const url=cfg.supabaseUrl||cfg.SUPABASE_URL;const key=cfg.supabasePublishableKey||cfg.supabaseAnonKey||cfg.SUPABASE_PUBLISHABLE_KEY||cfg.SUPABASE_ANON_KEY;const feedback=document.querySelector("#reset-password-feedback"),form=document.querySelector("#reset-password-form"),btn=document.querySelector("#reset-password-submit");if(!url||!key){feedback.textContent="Configuração do Supabase indisponível.";feedback.dataset.type="error";}else{const supabase=createClient(url,key,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});form.addEventListener("submit",async e=>{e.preventDefault();const a=document.querySelector("#reset-new-password").value,b=document.querySelector("#reset-confirm-password").value;if(a.length<6){feedback.textContent="A senha precisa ter pelo menos 6 caracteres.";feedback.dataset.type="error";return}if(a!==b){feedback.textContent="As senhas não coincidem.";feedback.dataset.type="error";return}btn.disabled=true;feedback.textContent="Salvando...";try{const{error}=await supabase.auth.updateUser({password:a});if(error)throw error;feedback.textContent="Senha alterada com sucesso. Você já pode voltar ao site.";feedback.dataset.type="success";form.reset()}catch(err){feedback.textContent=String(err?.message||"Não foi possível alterar a senha.");feedback.dataset.type="error"}finally{btn.disabled=false}})}